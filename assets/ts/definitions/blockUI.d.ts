// Type definitions for JQuery blockUI
// For usage in AMI dashboard only (only used parts are defined)
// Definitions by: Michael Odintsov

interface JQueryStatic {
    blockUI(any) : void;
    unblockUI() : void;
}